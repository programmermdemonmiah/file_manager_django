on: 
  push:
    branches: 
      - main

name: 🚀 Deploy website on push

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v4
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: http://66.85.139.234:2082/ #https://host.hostnavy.net:2083/
        username: marketsp #${{ secrets.ftp_username }}
        password: x3j7pE6t7E #${{ secrets.ftp_password }}
        server-dir: /File_Manager/